# 记忆系统设计

## 目标

记忆系统用于在八字、六爻、奇门、紫微问答之间共享稳定的用户画像、命盘事实和历史洞察，帮助后续分析减少重复提问并保持语境连续。

## 当前实现

- `memory_manager.py` 是核心实现，使用本地 JSON 文档作为主存储。
- `memory_organizer.py` 在 `ENABLE_ASYNC_ORGANIZER=true` 时处理异步整理事件。
- 本地向量记忆由 `ENABLE_VECTOR_MEMORY` 控制，依赖 `numpy`、`openai` 和 Embeddings 配置；缺失时会降级到结构化 JSON。
- Mem0/Qdrant 集成已废弃并从基础依赖中移除。

## 数据层

- Chart facts：从排盘结果中提取稳定命盘事实。
- Profile：沉淀用户长期偏好、背景和咨询习惯。
- Insights：保存各分析维度的最新结论和历史版本。
- Episodes：保存一次对话或占问中的短期事件。

## 业务入口

- `GET /api/memory/summary` 获取当前用户记忆总览。
- `POST /api/memory/items/<memory_id>/pin` 固定重要记忆。
- `DELETE /api/memory/items/<memory_id>` 删除单条记忆。
- `POST /api/memory/preferences` 更新记忆偏好。
- `POST /api/memory/rebuild` 重建当前用户记忆摘要。

调试型记忆接口 `/api/debug/*` 已移除。验证记忆行为应通过单元测试或业务 API 完成。

## 验证

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_memory_system -v
```

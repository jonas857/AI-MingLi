# 八字命理分析系统产品说明

## 当前能力

- Flask 单体应用，入口为 `app_simplified.py`。
- 页面包括 `/`、`/login`、`/home`、`/memory`、`/birth-input`、`/analysis`、`/liuyao`、`/qimen`。
- 八字排盘优先使用本地 Node 桥 `local_bazi_calculator.py` + `bazi_local_node.mjs`，失败后可回退到 `bazi_client.py` 的 MCP 链路。
- AI 解读由 `ai_analyzer.py`、`prompt_templates.py` 和 DeepSeek/Gemini 兼容接口完成。
- 本地账号由 `auth_manager.py` 管理，业务用户和记忆由 `user_manager.py`、`memory_manager.py` 管理。
- PDF 导出由 `pdf_report.py` 使用 ReportLab 生成。

## 主要 API

- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/auth/me`
- `GET /api/user/home`
- `GET /api/memory/summary`
- `POST /api/memory/items/<memory_id>/pin`
- `DELETE /api/memory/items/<memory_id>`
- `POST /api/memory/preferences`
- `POST /api/memory/rebuild`
- `POST /api/bazi/get`
- `POST /api/analysis/batch`
- `POST /api/analysis/start`
- `POST /api/analysis/interpretation`
- `POST /api/analysis/follow-up`
- `GET /api/ziwei/chat/history`
- `POST /api/ziwei/chat/send`
- `POST /api/ziwei/chart`
- `POST /api/liuyao/calculate`
- `POST /api/qimen/calculate`
- `GET /api/user/export-pdf`

开发者后台和 `/api/debug/*` 调试接口已移除；紫微排盘使用正式接口 `POST /api/ziwei/chart`。

## 关键配置

- `BAZI_CALC_PROVIDER=auto|local|mcp`
- `ENABLE_MCP_BAZI=true|false`
- `ENABLE_MCP_ZIWEI=true|false`
- `ENABLE_SHARED_MEMORY=true|false`
- `ENABLE_VECTOR_MEMORY=true|false`
- `ENABLE_ASYNC_ORGANIZER=true|false`
- `ENABLE_AI_CHART_FACTS_SUMMARY=true|false`

`user_data/*.json` 是当前项目数据的一部分，继续保留在仓库中。`local_data/`、`.env`、日志、缓存、虚拟环境和 Node 依赖不进入仓库。

## 常用命令

```powershell
.\.venv\Scripts\python.exe app_simplified.py
```

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_memory_system -v
```

```powershell
npm install
```
